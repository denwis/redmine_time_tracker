$('#ajax-modal').html('<%= escape_javascript(render :template => 'time_trackers/stop', :formats => [:html]) %>');
showModal('ajax-modal', '850px');
updateTimeTrackerMenu();
// Add remote to issue list headers
$("a[href^='/time_trackers?sort=']").attr('data-remote', 'true');
// Add modal dialog title
$('#ui-dialog-title-ajax-modal').text('<%= @issue.nil? ? l(:start_time_tracker).capitalize : @issue %>');
$('#ajax-modal').addClass('time-trackers-index');